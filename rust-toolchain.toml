[toolchain]
channel = "nightly"
components = ["rustfmt"]
# Use nightly for formatting to enable unstable rustfmt features
# This allows rustfmt to use unstable features like:
# - format_code_in_doc_comments
# - format_strings
# - format_macro_bodies
# - imports_granularity
# - group_imports
# - brace_style

