While this directory is named examples, its really a collection of test programs for the ferros-core library. 
Each program needs to run as its own process so using the normal test pattern of `cargo test` is not possible
for full testing. 